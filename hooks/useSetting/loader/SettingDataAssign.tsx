"use client";

// hooks
import { useSetting } from "../useSetting";

// types
import { type SettingDocument } from "@/common/types/documentation/settings/setting";

export default function SettingDataAssign({
  setting
}: {
  setting: SettingDocument;
}) {
  // const { onSetData } = useSetting();

  // onSetData(setting);

  return <></>;
}
