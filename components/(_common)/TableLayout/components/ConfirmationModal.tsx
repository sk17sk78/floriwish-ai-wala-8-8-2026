export default function ConfirmationModal({
  title,
  onCancel,
  onAction,
  triggerType
}: {
  title: string;
  onCancel: () => void;
  onAction: () => void;
  triggerType?: "normal" | "destructive";
}) {
  return <></>;
}
