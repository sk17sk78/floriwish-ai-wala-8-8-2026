// types
import { DynamicPageDocument } from "@/common/types/documentation/pages/dynamicPage";

export default function TableFormFields({
  initialDocument
}: {
  initialDocument?: DynamicPageDocument;
}) {
  return <></>;
}
