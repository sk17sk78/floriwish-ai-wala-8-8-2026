import { BlogArticleDocument } from "@/common/types/documentation/blog/blogArticle";

export const convertToRawBlogString = (
  data: BlogArticleDocument["layouts"] | undefined
): string => {
  return "";
};
